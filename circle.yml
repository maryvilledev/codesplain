test:
  override:
    - echo "test"

deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
        - sh publish.sh release $CIRCLE_TAG
  master:
    branch: master
    commands:
      - sh publish.sh dev
