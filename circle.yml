test:
  override:
    - echo "test"

deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
        - ./publish.sh codesplain
  master:
    branch: master
    commands:
      - ./publish.sh codesplain-dev
