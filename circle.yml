machine:
  node:
    version: 7.7.2

test:
  override:
    - echo "test"

deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
        - ./publish.sh release $CIRCLE_TAG
  master:
    branch: [fixCircle2, master]
    commands:
      - ./publish.sh dev
